import React from 'react';
import Layout from '../components/Layout';

const Blog = () => {
    return (
        <Layout pageTitle='Blog'>
            <div>Blog Page</div>
        </Layout>
    );
};

export default Blog;
